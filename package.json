{
  "name": "docker",
  "version": "1.0.0",
  "description": "###도커 이미지 빌드, docker build ```shell docker build -t [닉네임]/[이미지 이름] . ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": ""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1"
  }
}
